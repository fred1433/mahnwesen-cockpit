{
  "name": "mahnwesen-cockpit",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "description": "Vorgeschlagener Mahnwesen-Prozess: n8n orchestriert, der Code traegt zehn Kontrollen, eine Zahlung nach der Freigabe stoppt den Versand. Fiktive Beispieldaten, kein Versand.",
  "license": "MIT",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "lauf": "node src/lauf.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "pruefen": "npm run typecheck && npm run test && npm run lauf",
    "dienst": "node src/dienst.ts"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
